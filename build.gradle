apply plugin: 'java'

jar {
  baseName = 'pokedex-api'
  version = '0.1.0'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
  mavenCentral()
}

dependencies {
  testCompile("junit:junit")
}

task wrapper(type: Wrapper) {
  gradleVersion = '2.9'
}
